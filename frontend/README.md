# Frontend for starting-nodejs

Run locally:

```bash
cd frontend
npm install
npm run dev
```

By default the frontend expects the backend at `http://localhost:3000`. To change, set `VITE_API_URL` in your environment.
