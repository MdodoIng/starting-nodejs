# Server
PORT=3000
NODE_ENV=development

# Redis
REDIS_URL=redis://127.0.0.1:6379

# Auth
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=7d

# Score history retention (number of entries kept per user)
HISTORY_LIMIT=100
